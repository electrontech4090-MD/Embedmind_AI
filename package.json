{
  "name": "embedmind-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd apps/api && venv\\Scripts\\python -m uvicorn app.main:app --reload",
    "db:up": "docker-compose up -d"
  }
}
